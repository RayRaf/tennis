# Настройка WebSocket для трансляции счета товарищеских партий

## Что было реализовано

Добавлена функция трансляции счета товарищеских партий в реальном времени через WebSocket. Теперь когда начинается товарищеская партия, на странице событий клуба автоматически появляется раздел с текущим счетом, который обновляется после каждого нажатия на +1 или -1.

## Изменения в проекте

### 1. Установлены новые зависимости
- `channels==4.0.0` - Django Channels для поддержки WebSocket
- `daphne==4.1.0` - ASGI сервер для работы с Channels
- `channels-redis==4.2.0` - Redis layer для Channels (опционально)

### 2. Обновлены файлы конфигурации

#### settings.py
- Добавлен `daphne` в INSTALLED_APPS (первым в списке)
- Добавлен `channels` в INSTALLED_APPS
- Настроен `ASGI_APPLICATION`
- Настроен `CHANNEL_LAYERS` (используется InMemoryChannelLayer для простоты)

#### asgi.py
- Настроена маршрутизация для WebSocket подключений
- Добавлен ProtocolTypeRouter для обработки HTTP и WebSocket

### 3. Созданные новые файлы

#### tennis_app/consumers.py
WebSocket consumer для обработки подключений и трансляции счета:
- `FriendlyGameConsumer` - обрабатывает подключения к игровой комнате клуба
- Поддерживает события: `game_update` (обновление счета) и `game_end` (окончание игры)

#### tennis_app/routing.py
Маршрутизация WebSocket соединений:
- URL pattern: `ws/friendly_game/<club_id>/`

### 4. Обновленные файлы

#### staticfiles/js/friend_play.js
Добавлены функции для работы с WebSocket:
- `initWebSocket()` - инициализация WebSocket соединения
- `sendGameUpdate()` - отправка обновлений счета всем подключенным клиентам
- Обновления отправляются при каждом изменении счета (+1/-1)

#### templates/play_game.html
- Добавлено скрытое поле с ID клуба для WebSocket соединения

#### templates/club_detail.html
Добавлен раздел для отображения трансляции:
- Секция `#liveGameSection` - отображает текущий счет
- JavaScript код для подключения к WebSocket и обновления интерфейса
- Автоматическое отображение/скрытие раздела при начале/окончании игры

## Инструкции по установке

### 1. Установить зависимости

```powershell
cd c:\source\tennis\tennis\tennis
pip install -r requirements.txt
```

### 2. Запустить сервер

Теперь нужно использовать Daphne вместо стандартного runserver для поддержки WebSocket:

```powershell
cd c:\source\tennis\tennis\tennis
daphne -b 0.0.0.0 -p 8000 tennis.asgi:application
```

Или можно использовать стандартный runserver (Django 3.0+ поддерживает ASGI):

```powershell
python manage.py runserver
```

## Как использовать

1. **Откройте страницу клуба** в браузере (например, `/club/<club_id>/`)
   
2. **Начните товарищескую партию**:
   - Нажмите кнопку "⚔️ Товарищеская партия"
   - Выберите игроков
   - Нажмите "Начать игру"

3. **Наблюдайте за трансляцией**:
   - Откройте страницу событий клуба в другом окне/вкладке или на другом устройстве
   - При начале игры появится раздел "🔴 Идет товарищеская партия"
   - Счет будет обновляться в реальном времени при каждом нажатии +1/-1
   - После завершения игры раздел автоматически скроется через 3 секунды

## Технические детали

### Архитектура

```
Игрок в партии (friend_play.js)
    ↓ WebSocket
Consumer (FriendlyGameConsumer)
    ↓ Channel Layer (group broadcast)
Наблюдатели на странице клуба (club_detail.html)
    ↓ Обновление UI
```

### WebSocket URL
- Pattern: `ws://hostname/ws/friendly_game/<club_id>/`
- Группы: `friendly_game_{club_id}` - отдельная группа для каждого клуба

### Формат сообщений

**game_update** (обновление счета):
```json
{
  "type": "game_update",
  "game_data": {
    "game_type": "single",
    "score1": 5,
    "score2": 3,
    "player1": "Иван Иванов",
    "player2": "Петр Петров",
    "is_finished": false,
    "server": 1
  }
}
```

**game_end** (окончание игры):
```json
{
  "type": "game_end",
  "game_data": {
    "game_type": "single",
    "score1": 11,
    "score2": 7,
    "player1": "Иван Иванов",
    "player2": "Петр Петров",
    "winner": "Иван Иванов"
  }
}
```

## Улучшения на будущее

1. **Redis для production**: Замените InMemoryChannelLayer на RedisChannelLayer для работы с несколькими воркерами

```python
CHANNEL_LAYERS = {
    'default': {
        'BACKEND': 'channels_redis.core.RedisChannelLayer',
        'CONFIG': {
            "hosts": [('127.0.0.1', 6379)],
        },
    },
}
```

2. **Переподключение**: Добавить более умную логику переподключения при разрыве соединения

3. **Аутентификация**: Добавить проверку прав доступа в Consumer

4. **История партий**: Сохранять промежуточные обновления счета в базу данных

5. **Уведомления**: Добавить звуковые/push уведомления при изменении счета

## Отладка

Если WebSocket не работает:

1. Проверьте, что Daphne или ASGI-совместимый сервер запущен
2. Откройте консоль разработчика браузера и проверьте WebSocket соединения
3. Убедитесь, что путь в routing.py совпадает с путем в JavaScript
4. Проверьте, что club_id передается корректно в шаблон

## Команды для проверки

```powershell
# Проверить установку channels
python -c "import channels; print(channels.__version__)"

# Проверить установку daphne
python -c "import daphne; print(daphne.__version__)"

# Запустить миграции (если нужно)
python manage.py migrate

# Запустить тесты
pytest
```
